{
  "name": "parse-hostname",
  "version": "1.1.0",
  "license": "MIT",
  "repository": "LinusU/parse-hostname",
  "scripts": {
    "test": "standard && node test"
  },
  "devDependencies": {
    "got": "^8.1.0",
    "standard": "^10.0.3"
  }
}
